@model HomeIndexViewModel

@{
    ViewBag.Title = "Restaurant Info";
}

<h1>Restaurants</h1>

@foreach (var restaurant in Model.Restaurants)
{
    @Html.Partial("_Summary", restaurant);
}

<div>
    <a class="btn bnt-primary" asp-action="Create">Add Restaurant</a>
</div>
